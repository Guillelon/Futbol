@model Futbol.Models.Jugador

@{
    ViewBag.Title = "Delete";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<fieldset>
    <legend>Jugador</legend>

    <div class="display-label">Nombre</div>
    <div class="display-field">@Model.Nombre</div>

    <div class="display-label">Apellido</div>
    <div class="display-field">@Model.Apellido</div>

    <div class="display-label">Nacionalidad</div>
    <div class="display-field">@Model.Nacionalidad</div>

    <div class="display-label">Equipo</div>
    <div class="display-field">@Html.DisplayTextFor(_ => Model.Equipo).ToString()</div>
</fieldset>
@using (Html.BeginForm()) {
    <p>
        <input type="submit" value="Delete" /> |
        @Html.ActionLink("Back to List", "Index")
    </p>
}


